# Testbenches

This folder contains simple simulation testbenches for the RTL modules.

## Current status

The current testbench drives a few fixed 32-bit messages into `message_parser.v` and checks the parsed fields in simulation.
